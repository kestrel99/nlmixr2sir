% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sir-results.R
\name{sirSummary}
\alias{sirSummary}
\title{Summarize final SIR resampled parameter vectors}
\usage{
sirSummary(resampledMat, fit)
}
\arguments{
\item{resampledMat}{Numeric matrix of final resampled parameter vectors, one
row per vector and one column per parameter.}

\item{fit}{An nlmixr2 fit object used to obtain reference parameter
estimates.}
}
\value{
A data frame with columns \code{param}, \code{estimate}, \code{mean}, \code{sd}, \code{rse},
\code{rse_sd_scale}, and the percentiles \code{p2.5}, \code{p5}, \code{p10}, \code{p30}, \code{p50},
\code{p70}, \code{p90}, \code{p95}, \code{p97.5}. The percentile set is PsN's, derived from
prediction intervals 0, 40, 80, 90 and 95.

\code{rse} is a \strong{percentage}; PsN reports the same quantity as a fraction.
\code{rse_sd_scale} is \code{rse / 2}, the RSE of a variance expressed on the
standard-deviation scale. It is reported only for OMEGA \strong{diagonals} and
is \code{NA} everywhere else, including OMEGA off-diagonals: a covariance can
be negative or zero and has no standard-deviation counterpart, so halving
its RSE would not mean anything. For off-diagonal uncertainty, use the
empirical correlations in the \code{sdCorMatrix} attribute.
Note this differs from PsN, which halves everything that is
not a NONMEM THETA: nlmixr2 parameterises residual error on the SD scale
already, so halving \code{add.sd} would rescale a quantity that needs no
rescaling.

Attributes \code{covMatrix}, \code{corMatrix} and \code{sdCorMatrix} hold the empirical
covariance, correlation, and standard-deviation/correlation matrices.
}
\description{
\code{sirSummary()} computes empirical summary statistics from final SIR
resampled parameter vectors. It reports the reference estimate from the
input fit, empirical standard deviation and RSE, and percentile intervals
for each sampled parameter. The empirical covariance and correlation
matrices are attached as attributes.
}
